# 2026-09-17 晚间本地接续

本轮由用户重新发起本地并行工作，不沿用已结束的 09:00 期限。基线为 `13fd766`。工程实现使用 DeepSeek Flash；GPT-6 负责拆分任务、路线判断、代码审查和验收。当前环境没有可调用的 Codex 5.3，不把其他型号标成 5.3。

## 工作方向与独立审查

本轮集中解决连续生活的具体断点：退出时停止接收新决策、让已开始的决策完成写回、恢复一笔已付费但未应用的原回复，以及补齐烤炉实体碰撞。完成标准仍是居民自主需要或真实问题进入 GM 维护、原居民自然采用、存档重启后继续。

按用户要求创建了不继承聊天历史的 GPT-6 评审任务，只给原始产品目标，并禁止读取路线图、历史总结、私有目录和其他代理结论。内置委派多次返回模型容量不足；另用仅包含当前源码、没有 Git 历史与路线文档的导出目录启动独立原生 GPT-6 进程，仍返回容量不足。**独立审查未完成，没有“白 GPT-6 已通过”的结论。** 本页的优先级属于主 GPT-6 判断。

工程进程不是游戏里的 GM 身份，本轮工程委派不计为 10 GM 的实际世界参与。

## 已验证：烤炉碰撞

DeepSeek 为现有炉体补上同尺寸的实体碰撞：1.0 × 1.0 × 0.76 米。保留原外观、炉前工作位置、视线规则、面粉与口粮规则。

隔离副本最初缺少已编译 C# 依赖及三个启动纹理缓存，工作进程的测试未完成；600 秒到期后保留部分代码与未知总用量。主代理补齐已有运行依赖后执行候选验收，再合入本地代码复验：**66 项检查通过、0 次模型调用、测试进程完整退出，最终日志无错误。** 测量包括旧无碰撞炉体的穿入反例、炉后与侧面的实体阻挡、炉前可达、视线被真实墙壁遮挡、个人知识只在可见后产生，以及有限原料制作口粮的守恒。

实际工作位置最终误差约 0.075 米，低于 0.45 米到达半径。测试使用原语组成的物理场景与真实烘焙组件；未测真实 16 栋街区的炉位、导航烘焙或实际居民采用。研究世界尚未安装烤炉。

## 已验证：停止新决策后再退出

第一轮 DeepSeek 在 720 秒到期时留下三个文件的候选补丁，最终用量未知保留。主代理发现了字段/方法调用不一致、截止帧仍可能派发一个新决策、以及用工作线程结束推断模型已结算的错误，已交回原 DeepSeek 会话修正并保留上下文。

延迟回复、无响应有界退出、截止帧禁止新决策三个离线场景，在本地主仓复验 **44 项通过**。DeepSeek 同会话修正测试资源清理后，最终引擎日志没有资源泄漏或错误，所有测试进程完整退出。相关主仓回归：居民回合 29 项、决策上限 11 项、Python 账本与启动器 24 个测试通过。

默认截止后等待上限 45 秒，可在 0—120 秒配置；启动器将该等待明确计入引擎和本轮调用截止时间。身体已接受的行走、取料等任务保持持久 pending，不要求所有生活动作完成才结束。已被居民客户端自身放弃的请求仍需要原回复对账和补写，这个补丁没有解决所有请求超时，也不把旧 life-12 改写为通过。

## 已恢复：织工原回复

只读核对原世界、居民、回合、控制器代次、供应商操作编号、原请求内容摘要、响应摘要与计费用量，全部匹配。世界原始失败档案仍为 `provider_error`，没有已应用的重放记录。原始请求与完整回复均保留在忽略的私有目录，公开文档不包含私有理由或完整回复。

原控制器恢复工具会准备一次新决策，不能用于补写这条已结算回复。DeepSeek 新增离线提取与 Godot 写回工具，主代理指出并由原会话修正旧状态仍被拒绝、来源标记缺失、重复确认的身份校验顺序及输出文件碰撞等问题。最终主仓 **15 个 Python 测试、110 项 Godot 检查通过**；合并后停机 44 项、普通回合 29 项再次通过，日志无错误。提取只读账本，写回取得世界写锁、核对原文件摘要，再复用原决定和现有动作验证；效果、审查记录与档案在同一个保存事务内写入。

先在真实存档副本验证：织工原决定是向治疗师求助，`ask_help` 已应用，seq165 → seq166。原失败档案完整保留，新增一次已应用回复记录；其他九人的回合记录与面包师未完成取料任务保持不变。再次执行被旧源档摘要拒绝，文件字节不变；完整冷恢复通过。随后使用同一收据和代码在原研究存档执行，结果与副本一致；原存档的完整冷恢复也通过，十位身份、计划与 pending 保持，新增供应商调用 0、账本与 guard 文件摘要不变。

这证明一条真实旧决定得到恢复，不证明治疗师已经响应求助，也不证明之后的自主生活回合成功。原 `brain_run_failed` 保留在失败证据中，当前回合状态以 `settled` 为准。原始回复和私有理由没有公开。

DeepSeek 扩展回归时发现旧 `town_controller_recovery_acceptance.gd` 的 17 项检查中有 2 项失败，并在基线 `13fd766` 复现：旧测试要求过期回复不留档，以及预期旧错误码。这轮没有改写这两个预期，也没有把全仓测试称为全部通过；后续需要对齐旧控制器恢复与档案契约。

## 运行与计量边界

研究世界现为 seq166，源档 SHA-256 为 `5d4d848dc4048ec588c872b72a3511bfe23c796b31c3eca22aa315bc1cb719b6`，生活时间仍为 6087.88333333319 秒，9 个回合 settled／1 个 provider_error。没有启动新的真实 Kimi 生活回合，账本仍为 163 条 settled。渔夫的具体失败原因、现金合同后续履约、实际烘焙采用和持续无人接力 10+10 仍未完成。

DeepSeek 使用稳定工程指令前缀，后续修正续接原会话。本轮两条已取得最终计量的会话，按各自累计高水位去重后，输入 44,868,466、其中缓存输入 44,146,432、输出 296,303；缓存输入占 **98.39%**。这是原生会话工具链反复携带上下文的 token 计数，不是独立提示词长度或供应商最终账单。三次超时派发缺少各自结束时计量；其中停机和恢复会话之后取得更晚的累计值，炉体会话仍无最终总量。未知保留，不能将每次续接的累计值相加，也不把工程进程数当作 API 请求次数。

## 接下来的顺序

1. 查清渔夫在 durable gateway 前失败的具体环节，并验证恢复不会新买重复决定。排队等待占用客户端超时只是当前代码提示的候选原因，尚未证实。
2. 用当前主仓部署一个有界真实生活回合，观察停止派发、旧动作保存和冷恢复；先控制付费请求并发，保留十位居民身份，不把模型请求并发数当作居民数。
3. 连续生活稳定后验证真实街区炉位，让原居民自然发现、取料与烘焙，再交原 GM 复查。现金合同实际履约和持续 10+10 继续保留为未完成目标。

本轮只在本地集成与验证，尚未推送 GitHub。

## 2026-09-18 容量错误排查

之前几次“Selected model is at capacity. Please try a different model”来自独立 GPT-6-Astra 委派。原生记录显示先发生 WebSocket 采样超时，连续 5 次重试后回退 HTTPS，服务端返回该容量错误；没有返回可继续使用的结果。用户随后确认：当时使用的是另一个账号，切换账号后同一流程恢复正常。这说明根因应优先归为旧账号的模型容量／资格或账号会话状态，而不是这台电脑或项目代码；当前委派接口对旧会话还曾把 Astra 显示为 `Unknown model`，所以缓存清单里的可见性不能证明旧账号实际可调度。

当前账号读数显示普通使用允许，周窗口已用 38%，没有触发 spend 或 rate-limit 封锁；这只能说明当前账号没有额度封锁，不能反推旧账号状态。新建的 GPT-5.6-Luna 低推理探针成功启动，作为当前账号对照。官方 OpenAI 文档将模型暂时无容量归为服务端过载，建议遵循 `Retry-After` 并降低重试频率；本次不应无限重试旧账号的 Astra，也不应把 GPT-5.6-Luna 冒充成用户要求的独立 GPT-6 审查。

后续规则：需要独立 GPT-6 评审时，先用当前账号做一次单请求可用性探针；Astra 不可调度就记录账号、时间和实际错误，不重复派发。工程代码继续走 DeepSeek；路线判断必须记录当前主代理的实际型号和限制。本次恢复后的主代理会话已切换为 GPT-5 系列，因此本次不能声称由 GPT-6 完成方向审查。
