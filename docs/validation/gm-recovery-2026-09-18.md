# Bounded GM recovery and live continuation — H113

The two-hour user window starts at 2026-09-18 11:00:08 UTC and ends at 13:00:08 UTC (21:00:08 China time). Diagnosis, maintenance and simulation share this deadline. This is a bounded experiment, not proof of endless autonomous development. At this interim delivery, same-world simulation is running.

## Why the GM returned nothing

The previous observation mixed investigation with coding preparation. It issued 37 shell inspections and consumed at least 792,826 tokens before its 90-second deadline, without returning the required observation. The old final usage tail is still unknown and remains recorded. Explicit recovery acknowledged that uncertainty without replacing it with zero.

Observation now receives both complete pinned design documents, their hashes and bounded world evidence in one packet. Shell execution and waiting tools are disabled in this stage. Missing evidence must produce a bounded observation or no-action response; implementing a proposal belongs to a separate stage. Feedback uses the same evidence boundary. Exact GM-authored design reviews returned beside a scope are preserved inside it; conflicting declarations are rejected rather than invented or silently discarded.

Three real GM05 observations completed, including reuse of the same native session. The first completed in 37.5 seconds with 23,529 tokens and no shell calls. The next completed in 12.1 seconds with 34,305 additional tokens. Permanent usage attributes session deltas without double counting. This establishes successful bounded observation and continuation, not recovery of the old missing billing tail.

## One concrete change through review

GM05 proposed a finite public iron source after the smith's actual statement that he lacked iron for the carpenter's repair. A host-directed follow-up supplied the existing installation interface and bounded scope. GM05 claimed and authored one six-field JSON configuration: three units of salvage iron at a concrete street position. This follow-up is not presented as spontaneous invention or a canonical SAO material formula. The original extension preserves scarce resources, earned production, knowledge limits, identities and physical execution.

The first coding turn succeeded but used 839,976 tokens across many inspections. The opt-in `gm_runner.py code --json-artifacts` path addresses this configuration case: the model returns one to four approved JSON objects with shell tools disabled; the host checks all destinations, rejects escapes, aliases, extra files, non-objects, nonfinite numbers and oversized content, then writes the candidate and runs its declared tests. The real resumed GM session returned the same configuration in 9.4 seconds and 54,102 additional tokens. This mode is for fully specified JSON data; general engine changes still need the separate coding workflow and review.

A controlled trial copied the exact active world rather than reseeding it. The existing scene's fixed navigation moved a real resident body 7.72 metres, with a maximum sampled horizontal step of 0.023 metres. Personal line-of-sight observation enabled collection; each unit required 60.45 game seconds. Three units were collected, zero stock rejected further work, and production cold loading preserved earned inventory and depletion. All 38 checks passed. The harness directed this copy's walk and choices; it is not evidence of voluntary NPC adoption. The finite-material rule suite separately passed 58 checks, including idempotence, rollback, knowledge and no regeneration.

The primary assistant reviewed the configuration and installed it between closed live episodes through the existing transaction tool at life seq62. No NPC item, skill, Col, decision or knowledge was granted by installation. The original GM received the exact receipt in a real model turn and returned an acknowledgement bound to its hash, accepting the reported installation while explicitly retaining adoption as unproven.

## Continuing experiment and limits

The first ten-minute live episode completed 18 real NPC decisions and one GM observation, preserving the original ten identities and every earlier event and reply. The smith and carpenter used free conversation about repair terms. The source was installed after this episode; a second supervisor continues from the same save. Every episode exports a full checkpoint and permanent usage tags. Only one world writer runs at a time. The fixed deadline reserves time for in-flight settlement and process cleanup.

The initial 128-call estimate was revised to at most 256 NPC calls for the entire window after observing actual activity; the original CNY 10 new NPC-spend ceiling and existing cumulative ledger authorization remain in force. Already completed calls and charges are subtracted on resume. Unknown provider attempts are not automatically retried. GM observation is separately bounded; code and feedback receipts retain their actual token usage. Currency is not inferred from native token totals.

Remaining evidence to gather: voluntary discovery, collection and productive use of the iron; actual rather than spoken agreements; sustainable resources; and repeated author-led development without host interface hints. Residents may decline or never encounter the source. Four pre-existing navigation edge-overlap warnings remain; this trial does not establish that every route is clear.

Verification: 124 focused Python tests passed, followed by the five observation/artifact tests after an additional destination-alias check. The physical copy trial and 58 rule checks passed. Earlier failures remain recorded: a supervisor process-count field mismatch (no paid call), candidate path preflight refusal (no paid call), and a trial metric that sampled delayed world projection rather than the moving body. These were corrected and rerun; their failed runs are not counted as successes.

See [machine-readable evidence](gm-recovery-2026-09-18/checks.json), the [workflow](../../ROADMAP.md), and the [developer usage table](../../worlds/restart-20260918-01/usage/latest.md). Final same-world observations and accounting will be appended after the fixed stop.
