# H126 live material route

H126 resumed the canonical `shared:restart-20260918-01` save at seq250 under the existing bounded accounting chain. It made 12 serial Kimi calls, all settled for CNY 0.250603; the supplemental ledger had no reserved balance or unknown operation after shutdown. No prior request was retried.

The internal Godot capture fulfilled 600.0049 seconds, resolved its episode shutdown, and drained all gateway workers. The outer `run_town_model_validation.py` wrapper timed out during its close path and was terminated with exit 124. The capture evidence and canonical save remain usable, but this distinction prevents claiming a clean outer validation pass.

The real Carpenter route did not reach the source work gate. At seq263 the body was 0.7154 m from `street:iron-salvage-20260918`; the existing `turn:shared:carpenter:0:31` job had 5.6833/60 work seconds, remained pending, and the source stayed exhausted at initial3/recovered3/stock0. There was no `material_depleted` receipt, material transfer, response to Smith's inquiry, new edge-repair contract or completion. The earlier H125 fixture replay is still valid, so the next action is a non-paid inspection of the real street route and collision geometry. The known eight `particles is null` errors and four navigation edge warnings are recorded independently.
