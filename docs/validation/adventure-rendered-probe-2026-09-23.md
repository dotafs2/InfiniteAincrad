# Adventure rendered navigation probe — 2026-09-23

On a byte-identical disposable seq323 copy, the existing street scene baked its
navigation mesh from real colliders. `shared:baker` walked 22.57 metres to the
bounded gate in 1,005 frames at grounded collision steps below 0.023 m. An outside
target returned a zero direction and an explicit unreachable route.

The sidecar reducer rejected entry while a committed job was active and accepted
entry only after an explicit host closure. A seeded disposable encounter defeated
its guard in place; the same real `CharacterBody3D` then walked 22.18 metres back
to town before the separate defeat-retreat receipt was accepted. The copy's SHA was
unchanged, and the maintained seq323 world was untouched.

The known four navigation edge-merge warnings remain. This is a disposable scene
probe, not live adventure installation or resident adoption.
