# H106：对话历史上传与离线导航修复

用户要求继续工作、确保上传对话历史、遇到卡点告知。继续沿用离线范围，没有子代理、没有 NPC/GM 模型调用，没有使用另一台机器的 seq450 存档。

## 对话不再只剩流水摘要

已从本任务的精确本机日志导出用户消息、助手进度与最终答复，保留时间、角色、原文及逐条 SHA-256。首次快照包含 10 条用户消息、32 条助手消息，提交 `a269842` 后从 GitHub 实际下载两个原文文件，核对 SHA-256 与本地清单一致。本轮结束前再次增量导出；最新覆盖时间及条数以 [manifest](../history/conversations/2026-09-18-01a0b299/manifest.json) 为准。

[按时间阅读原文](../history/conversations/2026-09-18-01a0b299/conversation.md) · [固定接续说明](../history/conversations/2026-09-18-01a0b299/CONTINUE.md)。新增根目录 AGENTS.md，要求后续授权上传时一并刷新对话和接续说明，并核对远程。导出工具拒绝把历史缩短、换成另一任务、忽略残缺记录或直接导出未处理附件／疑似凭证。

范围明确为**当前任务的可见文字消息**。内部推理、系统指令、原始工具会话日志和私有世界／费用资料没有上传；没有声称补齐其他任务或另一台电脑的旧对话。当前快照之后的答复要在下一次刷新时补齐。

## 导航改了什么

- 用主线程解析碰撞几何后再烘焙的接口替换废弃调用，场景根、碰撞掩码与边界保持原值。迁移方式见 [Godot 官方接口说明](https://docs.godotengine.org/en/stable/classes/class_navigationserver3d.html#class-navigationserver3d-method-region-bake-navigation-mesh)。
- 显式写出引擎原来向上取整的有效烘焙半径。居民身体仍为 0.25 米半径；0.10 与 0.15 米网格下，原先实际使用的烘焙半径均约 0.30 米。没有缩小身体或减少避障空间。取整含义见 [NavigationMesh 官方说明](https://docs.godotengine.org/en/stable/classes/class_navigationmesh.html#class-navigationmesh-property-agent-radius)。
- 流程工具的 Python 恢复判断还保留旧的 `1e-9` 容差，本轮也改为精确比较。相邻浮点数、超过双精度整数范围的整数与布尔值混淆分别有回归；正式存档与最后两份冷恢复证据再次精确核对一致。

## 实测结果

| 范围 | 结果 | 边界 |
| --- | --- | --- |
| 同一离线十居民世界完整流程 | 8 个正常阶段、179 项检查通过；日志 0 错误、0 警告 | 脚本决策，真实权威规则、身体行走、劳动中断续做与最终完整冷恢复；不是模型自治 |
| 两档网格烘焙对比 | 10 项通过，针对同步问题另复查一次通过 | 真实碰撞地面与墙体；修改前后顶点和每个多边形完全相同；旧半径参考烘焙故意保留 2 条警告 |
| 原街区实际行走 | 14 项通过，4 段身体移动到达 | 市场／田野坡道往返、偏置出发点回家、水槽；另验证越界目的地不可达 |
| 16 栋生活街区独立公共初始档 | 40/40 公共地点路线、64/64 门窗检查通过 | 比较的是导航路径与门窗碰撞，不是该布局长期拥挤实走；非 seq450 |
| 对话导出、流程验证与预览入口 Python | 4 + 17 + 4 项通过 | 本轮实际运行，不重复累加上一轮的检查数 |

完整数值、源码摘要和范围见 [结果清单](navigation-and-history-2026-09-18/result-summary.json)。正常流程最终有限铁料仍为库存 1、累计取得 2，历史与未完劳动按原逻辑保留。未审阅发布的拒绝负例没有混入正常阶段通过数。

本轮流程原始证据：`C:\InfiniteAincrad\tmp\gpt6-sprint\integration\navigation-20260918\`。导航专项与旧代码对照：`C:\InfiniteAincrad\private\navigation-20260918\`。自有测试、浏览器渲染、构建与导入进程均退出；用户要求打开的浏览器窗口不由本轮关闭。

## 遇到的问题与剩余卡点

新烘焙测试首次在同一 World3D 切换第二档精度时查询到空路径。仅增加物理帧等待仍失败，诊断添加一次查询后又通过，表明时机不稳定；几何始终逐值相同。最终在测试查询前显式同步导航地图，两次检查均通过。三份早期失败保留，没有删除失败或放宽断言；未将这个测试时序调整混称为生产拥挤问题修复。

**16 栋街区仍有导航边缘合并警告，报告 4 处重合。** 用交付基线 `9fcd05c` 的原导航源码、同一公共预览档重新检查，同样复现；40 条路线与门窗结果在修改前后完全一致。警告数量由旧版的三类变为一类，剩下的几何重合没有隐藏或修改警告开关。

[旧代码日志](navigation-and-history-2026-09-18/quarter-baseline.log.txt) · [修改后日志](navigation-and-history-2026-09-18/quarter-current.log.txt) · [身体实走](navigation-and-history-2026-09-18/physical-routes.log.txt) · [烘焙等价](navigation-and-history-2026-09-18/bake-equivalence.log.txt)。

下一步继续定位具体重合边及贡献碰撞体，保留 40 条路线和身体间隙后再修正；不能只凭当前路径可达就关闭这个问题。历史 GPU 退出／粒子问题也没有因此结案。

原 seq450、GM 会话和费用账本仍在另一台电脑，真实供需、自然口粮转交和长期自主运行仍缺本机证据。涉及原世界的后续运行需要这些资料到位，当前离线工程可以继续。

当前主图已追加 H106，见 [ROADMAP.md](../../ROADMAP.md) 与 [可缩放图](local-flow-2026-09-18/flowchart.html)。H105 原结果清单仍保留当时的警告及当时 ROADMAP 的摘要，不能把 H106 修复后的状态倒写为 H105 原本无警告。
