# Axe-day demo runner — 2026-09-23

`AxeDayDemoRunner.run(host, mode)` returns UI-replayable scripted formal-resident steps from a disposable genesis copy. Modes are `success` and `missing_iron_refusal`. Dialogue offer and acceptance use the test-only dialogue fixture adapter; authoritative effects remain the existing `TownRuntime` and `town_turns` paths. `host_move` is a fixture arrival surrogate, not walking evidence.

Run `res://tests/axe_day_demo_runner_acceptance.gd` with the documented Godot 4.7.2 Mono wrapper. No provider calls or production saves are used.
